extends layout

block content
  -for (var i = 0; i < 3; i++)
    -for (var j = 0; j < 3; j++)
      -var classes = (j === 0 ? 'grid_4 prefix_2' : 'grid_4');
      div(class=classes)
        -classes = (j === 0 && i === 0 ? 'new_game' : 'board_selector')
        div(class=classes)
    .clear
